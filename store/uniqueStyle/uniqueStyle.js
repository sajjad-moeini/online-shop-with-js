let uniqueStyle= {
       title: 'استایل متفاوت و خاص خود را داشته باشید',
       btnTitle: 'مشاهده کنید'
}

const uniqueStyleReducer = (state=uniqueStyle,action)=>{
       switch(action.type){
              //
default:
       return state

       }
}

export default uniqueStyleReducer


